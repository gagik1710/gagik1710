server:
  port: 8080
spring:
  application:
    name: "Demo"
  datasource:
    url:
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: update
      use-new-id-generator-mappings: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect